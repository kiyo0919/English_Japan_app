<main>
  <div class="container">
    <div class="row">
      <div class="col-md-3">
        <%= render 'layouts/userinfo', post: @new_post %>
      </div>
      <div class="col-md-8 offset-md-1">
        <%= link_to @post.user.name, user_path(@post.user) %><br>
        <%= @post.body %><br>
        <%= attachment_image_tag @post, :image, :fill, 100, 150, format: 'jpeg' %><br>
        <% if @user == current_user %>
          <%= link_to "Delete", post_path(@post), method: :delete,'data-confirm' => "本当に消しますか？", class: "btn btn-sm btn-danger" %>
        <% end %>
      </div>
    </div>
  </div>
</main>